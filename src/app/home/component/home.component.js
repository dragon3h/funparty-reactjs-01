import React from 'react';

import {HomeContainer} from './home.styles';

const Home = (props) => {
  return (
      <HomeContainer>
        <h1>Home</h1>
      </HomeContainer>
  );
};

export default Home;